// # Publish argv rest proof
//
// Logline: publish reads --to from rest starting at 0.
//
// Setup: Covered by pm_argv_self_test.
//
// Beats:
//   1. --to is the dest.
//   2. Empty rest uses the default cache path.

import "cli/argv.oo";

fn fail(msg: String) {
    println("FAIL " + msg);
    process_exit(1);
}

pub fn main() {
    if !pm_argv_self_test() { fail("argv self test"); }
    println("argv_publish_proof: PASSED");
}

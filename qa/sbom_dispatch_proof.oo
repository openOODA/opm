// # Sbom dispatch proof
//
// Logline: sbom is its own handler. audit is audit.
//
// Setup: Covered by pm_argv_self_test.
//
// Beats:
//   1. sbom maps to sbom.
//   2. cat and more do not dispatch.

import "cli/argv.oo";

fn fail(msg: String) {
    println("FAIL " + msg);
    process_exit(1);
}

pub fn main() {
    if !pm_argv_self_test() { fail("argv self test"); }
    println("sbom_dispatch_proof: PASSED");
}

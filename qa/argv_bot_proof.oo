// # Bot argv rest proof
//
// Logline: bot reads the target dir from rest[0], not index 1.
//
// Setup: Covered by pm_argv_self_test.
//
// Beats:
//   1. First positional is the target dir.
//   2. --sign does not skip the dir.

import "cli/argv.oo";

fn fail(msg: String) {
    println("FAIL " + msg);
    process_exit(1);
}

pub fn main() {
    if !pm_argv_self_test() { fail("argv self test"); }
    println("argv_bot_proof: PASSED");
}
